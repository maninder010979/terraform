name: Terraform CI

on:
  push:
    branches:
      - 'feature/**'
  pull_request:
    types: [opened, synchronize, reopened]

env:
  TF_VERSION: '1.5.0'

jobs:
  terraform-init:
    name: Terraform Init
    runs-on: ubuntu-latest

    env:
      AZ_SERVICE_PRINCIPAL: ${{ secrets.AZ_SERVICE_PRINCIPAL }}
      AZ_SP_PASSWORD: ${{ secrets.AZ_SP_PASSWORD }}
      AZ_SUBSCRIPTION_ID: ${{ secrets.AZ_SUBSCRIPTION_ID }}
      AZ_TENANT_ID: ${{ secrets.AZ_TENANT_ID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print env vars
        run: |
          echo "ACTIONS_ID_TOKEN_REQUEST_URL: $ACTIONS_ID_TOKEN_REQUEST_URL"
          echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN: $ACTIONS_ID_TOKEN_REQUEST_TOKEN"

      - name: 'Azure Login'
        uses: azure/login@v1.6.1
        with:
          client-id: ${{ secrets.AZ_SERVICE_PRINCIPAL }}
          tenant-id: ${{ secrets.AZ_TENANT_ID }}
          subscription-id: ${{ secrets.AZ_SUBSCRIPTION_ID }}
          client-secret: ${{ secrets.AZ_SP_PASSWORD }}
          auth-type: SERVICE_PRINCIPAL

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: |
          ./create_backend_config.sh  

      # - name: Upload Terraform Init Data
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: terraform-init
      #     path: |
      #       .terraform
      #       .terraform.lock.hcl

  # terraform-validate:
  #   name: Terraform Validate
  #   runs-on: ubuntu-latest
  #   needs: terraform-init

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Setup Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TF_VERSION }}

  #     - name: Terraform Validate
  #       run: terraform validate

  # terraform-plan:
  #   name: Terraform Plan
  #   runs-on: ubuntu-latest
  #   needs: terraform-validate

  #   env:
  #     AZ_SERVICE_PRINCIPAL: ${{ secrets.AZ_SERVICE_PRINCIPAL }}
  #     AZ_SP_PASSWORD: ${{ secrets.AZ_SP_PASSWORD }}
  #     AZ_SUBSCRIPTION_ID: ${{ secrets.AZ_SUBSCRIPTION_ID }}
  #     AZ_TENANT_ID: ${{ secrets.AZ_TENANT_ID }}

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Azure Login
  #       uses: azure/login@v1
  #       with:
  #         client-id: ${{ secrets.AZ_SERVICE_PRINCIPAL }}
  #         tenant-id: ${{ secrets.AZ_TENANT_ID }}
  #         subscription-id: ${{ secrets.AZ_SUBSCRIPTION_ID }}
  #         client-secret: ${{ secrets.AZ_SP_PASSWORD }}

  #     - name: Setup Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TF_VERSION }}

  #     - name: Download Terraform Init Data
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: terraform-init

  #     - name: Terraform Plan
  #       run: terraform plan -out=tfplan

  #     - name: Upload Plan File
  #       uses: actions/upload-artifact@v3
  #       with:
  #         name: tfplan
  #         path: tfplan
